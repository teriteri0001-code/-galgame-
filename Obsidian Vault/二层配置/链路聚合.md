1，链路聚合意义：多根线（接口）捆绑到一起，汇聚带宽，热备份，负载均衡 
2：链路聚合模式：1手工模式（静态模式），2LACP模式（链路聚合控制协议） 
3：LACP实现过程（原理），以华为为例，A和B两台交换机，A的123口接B的123口 4.1，在A和B上创建eth-trunk线路，启用lacp，把123接口加入eth-trunk 4.2，配置A的lacp优先级比B高，A为主动端 
3.3，配置活动接口上限，最大8，如果3根线连接设置为2，则2根为活动链路，1根为备份链路 
3.4，配置接口的优先级，比如A的1和2口为活动接口，则1和2组成活动链路一起分担流量，3口为备份链路 
3.5，当活动链路中的1根down后，备份链路变成活动链路。旧的活动链路恢复后，可以设置是否抢占 
4，负载均衡设置 5.1，基于流（源目IP，mac，端口号）做负载均衡 
4.2，基于包，第1个包走1口，第2个包走2口，优点带宽利用率高，缺点数据包可能需要重组排序 
[SW1]vlan batch 10 20	                        //创建vlan
[SW1]interface vlanif 10	                    //进入vlanif接口
[SW1-Vlanif10]ip address 1.1.1.254 24	        //配置vlan的IP地址（网关）
[SW1-Vlanif10]quit	                            //退出vlanif接口视图
同理 vlan2
[SW1]interface eth-trunk 1	                        //创建并进入链路聚合接口
[SW1-Eth-Trunk1]port link-type trunk	            //配置接口类型
[SW1-Eth-Trunk1]port trunk allow-pass vlan 10	    //配置接口允许通过的vlan
[SW1-Eth-Trunk1]mode lacp-static	                //配置链路聚合模式为LACP
[SW1-Eth-Trunk1]max active-linkunmber 2	            //配置聚合组最大活动接口数量
[SW1-Eth-Trunk1]lacp selected priority	                //配置聚合组根据接口优先级选择活动接口
[SW1-Eth-Trunk1]trunkport gigabitethernet 0/0/1 to 0/0/3	        //将接口加入聚合组
[SW1-Eth-Trunk1]quit	                                            //退出链路聚合接口
在SW1上配置系统优先级为100，使其成为LACP主动端  
[SW1]lacp priority 100
[SW1]interface gigabitethernet 0/0/1	                        //进入G0/0/1接口
[SW1-GigabitEthernet0/0/1]lacp priority 100	                    //配置接口的LACP优先级
同理接口2
然后给接口4配接口和vlan
SW2创建vlan，创建eth-trunk 1，mode lacp（静态+-static），max act num，
trunkport 接口1-3，配置trunk允许vlanid通过，lacp selected priority，q
给接口1,2配置优先级，接口4画入vlan

![[Pasted image 20240719101727.png]]一季漠河打吧游
![[Pasted image 20240719101115.png]]